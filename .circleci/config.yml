version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15
    steps:
      - checkout
      - run: npm install
      - run: lerna bootstrap --hoist
      - run: lerna run test
      - run: lerna build
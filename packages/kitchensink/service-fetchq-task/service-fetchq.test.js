describe('service-fetchq-task', () => {
  beforeEach(fetchq.resetState);

  it('Should run a task', async () => {
    console.log('ok');
    // const res = await get.debug('/fetchq/append/q1/foobar');
    // expect(res).toMatchObject({ subject: expect.any(String) });
  });
});

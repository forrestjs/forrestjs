{
  "name": "@forrestjs/service-fastify-apollo",
  "description": "ForrestJS service which adds Apollo GraphQL Client to a Fastify server.",
  "version": "3.19.0",
  "author": "Marco Pegoraro <marco.pegoraro@gmail.com",
  "bugs": {
    "url": "https://github.com/forrestjs/forrestjs/issues"
  },
  "dependencies": {
    "@babel/runtime": "^7.12.5",
    "@forrestjs/hooks": "^3.19.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.1",
    "@babel/core": "^7.12.3",
    "@forrestjs/babel-preset-package": "^3.19.0",
    "@marcopeg/utils": "^2.0.1",
    "cross-env": "^7.0.2",
    "jest": "^26.2.3"
  },
  "homepage": "https://github.com/forrestjs/forrestjs/tree/master/packages/service-fastify-apollo#readme",
  "keywords": [
    "forrestjs",
    "service",
    "fastify",
    "static"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "repository": "https://github.com/forrestjs/forrestjs/tree/master/packages/service-fastify-apollo",
  "scripts": {
    "clean": "rm -rf node_modules && rm -f yarn.lock && rm -f package-lock.json && rm -rf lib && rm -rf index.js",
    "test": "jest",
    "build": "babel src --out-dir . --ignore **/__tests__",
    "build:to": "babel src --out-dir \"${TARGET}/node_modules/${npm_package_name}\" --ignore **/__tests__",
    "prepare": "cross-env NODE_ENV=production npm run build",
    "watch": "babel -w src --out-dir . --ignore **/__tests__",
    "watch:to": "babel -w src --out-dir \"${TARGET}/node_modules/${npm_package_name}\" --ignore **/__tests__"
  },
  "gitHead": "842378d848c42fb0c272e888ea538ec04335cfc4"
}

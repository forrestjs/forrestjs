const toQueueConfig = require("./utils/pipeline-to-queue");

module.exports = (_, { getContext }) => {
  // console.log("FOOOO");
  // const f = getContext("hasuraCQRS.pipelines.list").map(toQueueConfig);
  // console.log(f);
  return [];
};
